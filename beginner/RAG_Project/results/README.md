# Generated traces

Every `ask` run writes a canonical JSON trace here, including the question, generated answer, retrieved chunk IDs, retrieved context text, cosine scores, model names, configuration, and latency. These traces will become the inputs to the later Ragas and DeepEval phase.
