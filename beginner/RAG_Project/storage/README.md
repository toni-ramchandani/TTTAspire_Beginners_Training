# Generated index

The `build` command writes a provider- and embedding-model-specific JSON index here. Generated indexes are intentionally excluded from source control because they can be reproduced from `documents/`.
